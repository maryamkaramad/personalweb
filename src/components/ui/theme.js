import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  direction: 'rtl',
  palette: {
    primary: {
      main: '#673AB7'
    },
    secondary: {
      main: '#E040FB'
    }
  },
  typography: {
    fontFamily: 'vazir, roboto'
  }
})
